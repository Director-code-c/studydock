{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "StudyDock desktop window with no native command permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
